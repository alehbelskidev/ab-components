import { renderHook, act } from "@testing-library/react-hooks"
import { useCalendar } from "./use-calendar"

describe("useCalendar hook", () => {
	it("Throws error when initialized without context", () => {
		expect(true).toBe(true)
	})
})
